plugins {
    id 'java'
    id 'application'
}

group = 'th.rosenheim.oop'
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

application {
    mainClass = 'th.rosenheim.oop.App'
}

test {
    useJUnitPlatform()
}